/*
Contains all user and forum information
	-stores usernames and passwords
		-new users must have unique names
		-prompts user to enter different name or password if the password does not match an existing username
		-(Optional) privilege levels for users
	-stores the forum (which stores all the threads and posts)
	-opens a socket that allows remote connections to access the forum
		-(Optional) users with certain privilege levels can modify the forum
		-(Optional) users can modify own posts
*/

package server;

public class Server {

}